import { HealthRecord, IPrescription, IPrescriptionDetails, MedicationHistoryRecord, PatientMedicalHistory } from "models/health_record";
import { PayloadAction } from "@reduxjs/toolkit"
import { take } from "redux-saga/effects";


